[phases.setup]
nixPkgs = ["python312", "postgresql"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python scripts/init_db.py"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
